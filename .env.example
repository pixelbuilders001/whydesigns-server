# Server Configuration
NODE_ENV=development
PORT=5000

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/why-designers

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=7d

# Bcrypt Configuration
BCRYPT_ROUNDS=10

# CORS Configuration
CORS_ORIGIN=*

# Rate Limiting Configuration
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# AWS S3 Configuration
# Note: Make sure to configure your S3 bucket for public access by adding a bucket policy
# See API_DOCS.md for detailed bucket configuration instructions
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_REGION=us-east-1
AWS_S3_BUCKET_NAME=your-bucket-name

# AWS SES Configuration (Email Service)
# Note: Make sure to verify your sender email in AWS SES console
# For production, move SES out of sandbox mode to send to any email
AWS_SES_REGION=us-east-1
AWS_SES_FROM_EMAIL=noreply@why-designers.com
AWS_SES_FROM_NAME=Why Designers

# Google Calendar Configuration (for Booking System)
# Note: You need a Google Service Account to use this feature
# 1. Create a service account in Google Cloud Console
# 2. Download the JSON key file
# 3. Share your Google Calendar with the service account email
# 4. Set permission to "Make changes to events"
# Format private key as single line with \n for line breaks
GOOGLE_CLIENT_EMAIL=test@test0234565.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----test-----END PRIVATE KEY-----\n"
GOOGLE_CALENDAR_ID=resources.aman@gmail.com

# Timezone Configuration
TIMEZONE=UTC
