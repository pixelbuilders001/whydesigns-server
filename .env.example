# Server Configuration
NODE_ENV=development
PORT=5000

# DynamoDB Configuration
# For AWS: Use IAM roles (no need to set ACCESS_KEY_ID and SECRET_ACCESS_KEY if using EC2/ECS with IAM roles)
# For local development: Set credentials or use DynamoDB Local
AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-aws-access-key-id  # Already set below for S3
# AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key  # Already set below for S3
# Optional: For DynamoDB Local development (default: http://localhost:8000)
# DYNAMODB_ENDPOINT=http://localhost:8000
# Optional: Table name prefix for different environments (dev, staging, prod)
DYNAMODB_TABLE_PREFIX=why-designers

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=7d

# Bcrypt Configuration
BCRYPT_ROUNDS=10

# CORS Configuration
CORS_ORIGIN=*

# Rate Limiting Configuration
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# AWS S3 Configuration
# Note: Make sure to configure your S3 bucket for public access by adding a bucket policy
# See API_DOCS.md for detailed bucket configuration instructions
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_REGION=us-east-1
AWS_S3_BUCKET_NAME=your-bucket-name

# AWS SES Configuration (Email Service)
# Note: Make sure to verify your sender email in AWS SES console
# For production, move SES out of sandbox mode to send to any email
AWS_SES_REGION=us-east-1
AWS_SES_FROM_EMAIL=noreply@why-designers.com
AWS_SES_FROM_NAME=Why Designers

# Timezone Configuration
TIMEZONE=UTC
